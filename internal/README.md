# internal

Private application code that should not be imported by external projects.

## Structure

- `api/` – Figma API client and authentication
- `parser/` – URL parsing and node ID extraction
- `export/` – Asset export logic and formatting